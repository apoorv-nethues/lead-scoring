# Minimal dependencies for the Lead Scoring demo API only.
# Excludes matplotlib, seaborn, scipy, xgboost to reduce build size and avoid download timeouts.
catboost==1.2.8
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
joblib==1.5.3
numpy>=2.0.0
pandas>=3.0.0
